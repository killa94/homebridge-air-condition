{
  "name": "homebridge-air-condition",
  "version": "1.0.31",
  "description": "An example accessory plugin for homebridge written in Typescript",
  "main": "dist/accessory.js",
  "scripts": {
    "build": "tsc && npm version patch --no-git-tag-version && sudo npm install -g ."
  },
  "engines": {
    "homebridge": ">=1.0.0"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "devDependencies": {
    "@types/node": "10.17.19",
    "typescript": "3.9.7",
    "homebridge": "^1.1.1"
  },
  "dependencies": {
    "axios": "^0.19.2"
  }
}
